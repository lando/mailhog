# Disable the "core" mailhog plugins
disablePlugins:
  - lando-mailhog

# Make sure we are dogfooding this plugin for testing purposes
plugins:
  "@lando/mailhog": /home/runner/work/mailhog/mailhog

# Disable metrics reporting
stats:
  - report: false
    url: https://metrics.lando.dev
